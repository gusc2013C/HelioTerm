interface:
  display_name: "HelioTerm"
  short_description: "Universal compressed terminal transport"
  default_prompt: "Use $helioterm to run every non-interactive terminal command through compressed direct, supervised, or background execution."
