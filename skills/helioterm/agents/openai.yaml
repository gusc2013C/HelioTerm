interface:
  display_name: "HelioTerm"
  short_description: "Zero-model direct semantic terminal"
  default_prompt: "Use $helioterm direct mode. On Windows, use system powershell.exe for its one local call."
