interface:
  display_name: "HelioTerm"
  short_description: "Default zero-model terminal compressor"
  default_prompt: "Use $helioterm for bounded tests, checks, Git, search, source/config inspection, dependency, version, benchmark, and process observations; batch up to four compatible operations."
policy:
  allow_implicit_invocation: true
